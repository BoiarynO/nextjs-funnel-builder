# Documentation

This folder mirrors the project source structure. Each subfolder corresponds to a main source directory; documentation files describe modules in that area.

| Folder     | Source      | Description                    |
|-----------|-------------|--------------------------------|
| `stores/` | `stores/`   | Zustand stores and their API   |
| *(future)* | `utils/`, `components/`, … | Module docs as added |
