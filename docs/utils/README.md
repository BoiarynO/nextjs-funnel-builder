# utils

**Source:** `utils/`

Helpers: `funnelsStorage` (localStorage load/save), `config/limits`, `variables`, `formatting/formatTranslationKey`. See **SYSTEM-DESIGN.md** for persistence and config.
