# components

**Source:** `components/`

React components grouped by role: `views/` (page-level), `layout/` (sidebar, funnel content, editor), `ui/` (buttons, inputs, etc.). See **SYSTEM-DESIGN.md** for structure and responsibilities.
